'use strict';

angular.module( 'dynamic-tree' ).constant('CONSTANTS', (function() {

    return {
        TREE_STATE: 'treeState',
        RECURSIVE: 'Recursive',
        ITERATIVE: 'Iterative'
    };
})());